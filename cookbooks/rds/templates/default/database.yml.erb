<%= @environment %>:
  adapter:   <%= @adapter %>
  database:  '<%= @database %>'
  username:  '<%= @username %>'
  password:  '<%= @password %>'
  host:      '<%= @host %>'
  pool: 10
  reconnect: true
